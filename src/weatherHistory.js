export function drawWeatherHistory() {
    const ol = document.querySelector(".weatherList");
    const li = document.createElement("li");
   // const lastCity = window.localStorage.getItem("city");
    li.value
    ol.appendChild();
}